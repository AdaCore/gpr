with "gpr2";

project Agg_Conf is
   for Object_Dir use "obj";
   for Exec_Dir use ".";
   for Main use ("main.adb");

   package Compiler is
      for Switches ("Ada") use ("-g", "-O1", "-gnatwu");
   end Compiler;
end Agg_Conf;
