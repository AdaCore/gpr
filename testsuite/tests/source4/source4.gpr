with "gpr2";

project Source4 is
   for Object_Dir use "obj";
   for Exec_Dir use ".";
   for Main use ("main.adb");

   package Compiler is
      for Switches ("Ada") use ("-g");
   end Compiler;
end Source4;