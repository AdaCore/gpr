with "shared";

library project P2 extends all "base1" is
   for Source_Dirs use ("src/p2");
   for Object_Dir use "obj/p2";
   for Library_Dir use "lib/p2";
   for Library_Name use "p2";

   package Naming is
      for Specification ("Foo") use "foo.ada" at 1;
      for Body ("Foo") use "foo.ada" at 2;
   end Naming;
end P2;