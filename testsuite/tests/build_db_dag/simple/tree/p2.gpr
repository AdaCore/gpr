library project P2 is
   for Source_Dirs use ("src2");
   for Object_Dir use "obj2";
   for Library_Dir use "lib2";
   for Library_Name use "p2";

   package Naming is
      for Implementation ("Foo") use "foo.ada" at 2;
      for Specification ("Foo") use "foo.ada" at 1;
   end Naming;
end P2;