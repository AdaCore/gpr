project Shared is

   for Runtime ("C") use "foo";
   for Runtime ("Ada") use "bar";

   package Linker is
      for Default_Switches ("C") use ("c_switch");
      for Default_Switches ("Ada") use ("ada_switch");
   end Linker;

end Shared;
