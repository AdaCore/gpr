description: |
  A Rust executable linking a static encapsulated Ada library whose binder
  records a switch for a library GPR knows nothing about.
driver: python_script
control:
- [ SKIP, "not env.rust", "requires a Rust/Cargo toolchain" ]
