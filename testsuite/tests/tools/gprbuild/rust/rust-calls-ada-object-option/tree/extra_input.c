/*  Nothing but a symbol. The object file this produces is not needed by
    anything: what the test needs is a real file for the link to accept, so
    that the option the binder recorded can be checked on a build that
    succeeds.  */

int extra_input_marker;
