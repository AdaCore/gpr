with "demo";

project App is

   Src_Dir := external ("SRC_DIR", "app");

   for Source_Dirs use ("../src/" & Src_Dir);
   for Object_Dir use "obj/app";
   for Exec_Dir use "..";
   for Main use ("main.adb");

end App;
