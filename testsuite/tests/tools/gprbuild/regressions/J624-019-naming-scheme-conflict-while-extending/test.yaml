description: Regression test naming scheme clashing with extended sources (main) with a different naming scheme 
driver: python_script
timeout: 800
control:
- [XFAIL, "True", "Current limitation of gprbuild2 - We need to discover dependencies"]
