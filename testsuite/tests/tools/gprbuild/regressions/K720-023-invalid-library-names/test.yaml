description: Regression test for checking the Library_Name for forbidden values
driver: python_script
timeout: 800
control:
- [SKIP, "True", "Current limitation of gprbuild2 - We are not parsing the Library_Name of libraries to check for invalid chars"]
