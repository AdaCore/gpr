project Main is
   for Languages use ("Ada");
   for Source_Dirs use ("src");
   for Object_Dir use "obj";

   for Main use ("main.adb");

   package Binder is
      for Default_Switches ("Ada") use ("-O=1.bind", "-O=2.bind");
   end Binder;
end Main;