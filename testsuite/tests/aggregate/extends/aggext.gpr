with "gpr2";

project AggExt is
   for Object_Dir use "obj";
   for Exec_Dir use ".";
   for Main use ("main.adb");

   package Compiler is
      for Switches ("Ada") use ("-g", "-O1");
   end Compiler;
end AggExt;
