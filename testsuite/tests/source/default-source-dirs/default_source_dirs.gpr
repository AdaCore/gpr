with "gpr2";

project default_source_dirs is
   for Main use ("main.adb");
   for Object_Dir use "obj";
   for Exec_Dir use ".";
   for Source_Dirs use (".");

   package Compiler is
      for Switches ("Ada") use ("-g");
   end Compiler;

end default_source_dirs;
