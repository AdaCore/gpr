library project Lib is

   for Source_Dirs use ("src");
   for Library_Dir use "lib";
   for Object_Dir use "obj";
   for Library_Name use "cg";

   package Compiler is
      for Default_Switches ("Ada") use ("-fcallgraph-info=su,da");
   end Compiler;

end Lib;
