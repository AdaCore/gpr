project Matrix is
   for Languages use ("Ada", "C", "Fortran");
   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ("main.adb");

   package Compiler is
      for Default_Switches ("Ada") use ("-gnat05");
   end Compiler;

end Matrix;
