def build():
    # customer scenario
    anod_build("libgpr2", qualifier="bare")
    anod_build("libgpr2")
    anod_build("gpr2")

def debug_build():
    # customer scenario
    anod_build("libgpr2", qualifier="bare,dbg")
    anod_build("gpr2", qualifier="dbg")
